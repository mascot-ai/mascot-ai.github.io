---
// A captured app screenshot for use inside <BrowserFrame>.
const { src, alt, width = 1440, height = 723 } = Astro.props;
---
<img src={src} alt={alt} width={width} height={height} loading="lazy" class="block w-full" />
